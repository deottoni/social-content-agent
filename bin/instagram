#!/usr/bin/env python3
"""Entry point: `bin/instagram --brand <slug> <command>`. Add this repo's bin/ to PATH to type `instagram ...`."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from instagram_os.cli import main  # noqa: E402

sys.exit(main())
